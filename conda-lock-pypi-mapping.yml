# Package-name equivalences used when a dependency is named differently
# on PyPI and conda.
#
# This file is an input to the ai-research-env conda-lock build process.
# Most packages do not need an entry because their PyPI and conda names match.

torch:
  conda_name: pytorch
  conda_forge: pytorch
  pypi_name: torch
